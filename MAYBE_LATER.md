# Maybe Later

- Replace CLI review with Telegram bot interactions and inline actions.
- Replace mock workers with API-backed and crawler-based discovery workers that enforce `SearchFilters`.
- Add durable checkpointing.
- Add embedding-based cross-run deduplication.
- Add scheduling, metrics, multilingual support, and operational dashboards.
- Complete `AUGMENT` behavior so `augment_dispatch` updates state and accumulates additional candidates.
